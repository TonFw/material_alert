require 'spec_helper'

describe MaterialAlert do
  it 'has a version number' do
    expect(MaterialAlert::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
